[project]
name = "bibliotheque"
version = "0.8.0"
description = "Gestionnaire de bibliothèque personnelle (fil rouge, étape 08)"
requires-python = ">=3.14"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/bibliotheque"]
