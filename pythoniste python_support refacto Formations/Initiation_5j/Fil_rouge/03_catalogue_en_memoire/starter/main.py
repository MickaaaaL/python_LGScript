"""Étape 03 — Catalogue en mémoire (starter).

À compléter : chaque livre devient un dict avec titre, auteur, annee,
isbn et date_ajout (remplie automatiquement avec
``datetime.date.today().isoformat()``). Ajouter l'option de menu (3)
pour compter les livres.
"""

# TODO : importer ``date`` depuis ``datetime``

# TODO : reprendre le code de l'étape 02 et l'adapter :
#   - chaque livre est un dict à 5 clés
#   - menu enrichi : (1) ajouter, (2) afficher, (3) compter, (q) quitter
