[project]
name = "bibliotheque"
version = "0.7.0"
description = "Gestionnaire de bibliothèque personnelle (fil rouge, étape 07)"
requires-python = ">=3.14"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/bibliotheque"]
