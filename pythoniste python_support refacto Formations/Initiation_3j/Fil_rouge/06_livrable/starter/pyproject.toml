[project]
name = "recettes"
version = "0.6.0"
description = "Carnet de recettes (fil rouge, étape 06)"
requires-python = ">=3.14"
dependencies = []

# TODO: ajouter [project.optional-dependencies] dev = [...]
# TODO: ajouter [project.scripts] recettes = "recettes.__main__:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/recettes"]
